export type NavLink = {
  label: string;
  href: string;
};

export type Step = {
  eyebrow: string;
  title: string;
  description: string;
};

export type Activity = {
  title: string;
  neighborhood: string;
  vibe: string;
};

export type FaqItem = {
  question: string;
  answer: string;
};

export type Highlight = {
  title: string;
  description: string;
};
